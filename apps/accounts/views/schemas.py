from apps.accounts.filters import (
    CATALOG_TAG_FOREST,
    CATALOG_TAG_PROMO,
    CATALOG_TAG_RAILWAY,
    CATALOG_TAG_START_SALES,
)

_PROPERTY_TAGS_CATALOG_HINT = (
    'Для попадания в фильтры публичного каталога (`GET .../catalog/properties/`) добавьте в `tags` '
    '**точно такие** строки (как в справочнике на backend; при поиске регистр не важен):\n'
    f'- `{CATALOG_TAG_PROMO}` — фильтр `promo=true`\n'
    f'- `{CATALOG_TAG_START_SALES}` — `start_sales=true`\n'
    f'- `{CATALOG_TAG_FOREST}` — `forest_access=true`\n'
    f'- `{CATALOG_TAG_RAILWAY}` — `near_railway=true`\n'
    'Любые другие имена тегов тоже можно передавать — они просто не привязаны к этим query-параметрам.'
)

_PROPERTY_BODY_DESCRIPTION = (
    '**Рекомендуется `multipart/form-data`:** удобно прикреплять файлы. Также поддерживается `application/json` '
    '(без загрузки файлов или с URL вне этого API).\n\n'
    '- Текстовые поля как в форме на сайте.\n'
    '- **images** — одно или несколько полей с именем `images` (несколько файлов).\n'
    '- **tags** — массив **строк**, как **images**: в Swagger «Add string item» / повтор поля `tags` в form-data; '
    'формат JSON в теле: `["тег1","тег2"]` или `[{"tag_name":"имя"}]`. '
    'Переданный набор задаёт **текущие** теги объекта (полная замена из этого запроса для поля `tags`). '
    'Отдельно только теги: `PUT /properties/{id}/tags/`.\n\n'
    f'{_PROPERTY_TAGS_CATALOG_HINT}\n\n'
    '- **category_id** — обязательно при создании; список: `GET .../catalog/categories/`. '
    'Вложенные объекты (JSON или строка JSON в `multipart`): **`residential_details`** (все категории кроме '
    'земли) и **`land_plot_details`** (только slug `land_plot`). При **PATCH** переданные поля внутри блока '
    'дополняют уже сохранённые.\n'
    '- **district_id**, **highway_id** — из публичных справочников `GET .../catalog/districts/` и `.../catalog/highways/`; '
    '**address** — только вручную (улица, дом).\n'
    '- **area** — площадь дома/объекта, м²; **land_area** — участок, сот.; **distance_to_mkad_km** — до МКАД, км; '
    '**lat**, **long** — широта и долгота для карты (WGS84; в БД хранятся как latitude / longitude).\n'
    '- Флаги посёлка/локации: `has_asphalt_roads`, `has_street_lighting`, `has_guarded_territory`, `near_shops`, '
    '`near_school_kindergarten`, `near_public_transport` (boolean).\n'
    '- Коммуникации по отдельности: `electricity_supply`, `water_supply`, `sewage_type`, `heating_type`, '
    '`internet_connection`; поле **communications** — краткое резюме строкой.\n'
    '- **property_type:** по возможности не передавайте — выставляется от **category_id**; иначе legacy: '
    '`land` | `house` | `apartment` | `commercial` | `other`.'
)

_PROPERTY_DETAIL_DESCRIPTION = (
    'То же тело, что и при создании: **multipart/form-data** или JSON. '
    'При **PUT** нужно передать полный набор полей; при **PATCH** — только изменённые. '
    'Если передать `images` (включая пустой список), старые фото заменяются; без поля `images` файлы не трогаются.\n\n'
    f'{_PROPERTY_TAGS_CATALOG_HINT}'
)

_CATALOG_DESCRIPTION = (
    '**Назначение:** публичная витрина объявлений о недвижимости. В ответе только карточки со статусом '
    '**«Опубликован»** (после одобрения в админке). Черновики и объекты «на модерации» или «отклонён» '
    'в этом списке **никогда не отображаются**. Отдельная карточка: `GET .../catalog/properties/{id}/` — то же '
    'правило: неопубликованный id даст **404**.\n\n'
    '**Аутентификация:** JWT **не обязателен** для чтения списка. Если передать заголовок **Authorization: Bearer** '
    'с access-токеном, '
    'в каждом объекте будут актуальные поля **`is_favourite`** и **`is_compare`** для текущего пользователя. '
    'В Swagger UI нажмите **Authorize** и вставьте токен, иначе интерфейс может не прикрепить заголовок к запросу.\n\n'
    '**Сортировка:** по дате создания объявления, сначала новые (`-created_at`).\n\n'
    '**Пагинация (drf):** `limit` — размер страницы (по умолчанию из настроек), `offset` — смещение.\n\n'
    '---\n'
    '**Query-фильтры** (все необязательны; условия объединяются логическим **И**):\n'
    '- **`category`** — числовой **id** категории из справочника `GET .../catalog/categories/`.\n'
    '- **`category_slug`** — то же по **slug** (`new_building`, `land_plot`, `suburban`, …); регистр не важен. '
    'Не используйте одновременно с `category`, если нужен один критерий.\n'
    '- **`property_type`** — унаследованный тип объекта для фильтра: `land`, `house`, `apartment`, `commercial`, `other`. '
    'Обычно совпадает с категорией, но может отличаться на старых данных.\n'
    '- **`district`** — **id** района из `GET .../catalog/districts/`.\n'
    '- **`highway`** — **id** шоссе из `GET .../catalog/highways/`.\n'
    '- **`area_min`**, **`area_max`** — диапазон площади дома/объекта в **м²** (поле `area` объекта ≥ min и/или ≤ max).\n'
    '- **`land_area_min`**, **`land_area_max`** — диапазон площади участка в **сотках**.\n'
    '- **`distance_to_mkad_max`** — не дальше указанного числа **км** от МКАД: `distance_to_mkad_km ≤ значение` '
    '(удобно для загорода).\n'
    '- **`price_min`**, **`price_max`** — цена объекта в **рублях** (включительно по переданным границам).\n'
    '- **`has_asphalt_roads`**, **`has_street_lighting`**, **`has_guarded_territory`**, **`near_shops`**, '
    '**`near_school_kindergarten`**, **`near_public_transport`** — булевы признаки инфраструктуры/посёлка; '
    'передайте `true` или `false`.\n'
    '- **`promo`**, **`start_sales`**, **`forest_access`**, **`near_railway`** — при **`true`** оставляются только объекты, '
    'у которых среди тегов есть строка (без учёта регистра): «Акция», «Старт продаж», «С выходом в Лес», '
    '«Ж/д станция рядом» соответственно.\n'
    '- **`actual_offers`** — при **`true`** только объекты с галочкой **«Актуальные предложения»** в админке '
    '(карусель на главной).\n\n'
    'Подсказки по тегам для агентов (при сохранении объекта) см. в описании создания объекта.'
)

_AGENT_LIST_FILTERS_DESCRIPTION = (
    '**Query-фильтры личного списка** (`GET`, только свои объекты агента):\n'
    '- **`status`** — `moderation` (на модерации), `published` (опубликован), `rejected` (отклонён). '
    'Без параметра возвращаются объекты **всех** статусов.\n'
    '- **`category`** — фильтр по **id** категории витрины (`category_id`), как в справочнике категорий.\n'
    '- **`category_slug`** — фильтр по **slug** категории (точное совпадение без учёта регистра).\n'
    '- **`actual_offers`** — `true` / `false`: только объекты с флагом «Актуальные предложения» или без него.\n\n'
    'Пагинация: **`limit`**, **`offset`**.'
)

_LISTING_UNITS_DESCRIPTION = (
    '**Назначение:** список лотов/квартир внутри одного объекта ЖК (блок «Планировка и цены» на сайте). '
    'Родительский объект определяется **`listing_pk`** в URL (тот же id, что в каталоге).\n\n'
    '**Доступ:** публично, без JWT. Родительское объявление **должно быть опубликовано**; иначе ответ **404** '
    '(как для `GET .../catalog/properties/{id}/`). Если объект опубликован, но лотов в базе нет — **200** и пустой '
    'список в пагинации.\n\n'
    '**Пагинация:** `limit`, `offset`.\n\n'
    '**Query-фильтры** (опционально, логическое **И**; те же имена, что и у `.../units/summary/`):\n'
    '- **`q`** — текстовый поиск по полям: подпись карточки (`title`), корпус, группа планировки (`layout_label`), '
    'отделка (`finishing`). Подстрока, без учёта регистра.\n'
    '- **`building`** — корпус/литер: вхождение подстроки в поле «корпус».\n'
    '- **`layout_label`** — группа аккордеона на фронте (например «Студия», «2-комнатная»): **точное** совпадение '
    'строки без учёта регистра.\n'
    '- **`rooms`** — точное число комнат (для студии см. `is_studio`).\n'
    '- **`rooms_min`**, **`rooms_max`** — диапазон по числу комнат.\n'
    '- **`is_studio`** — `true` / `false`: только студии или только не-студии.\n'
    '- **`price_min`**, **`price_max`** — цена лота в **рублях**.\n'
    '- **`area_min`**, **`area_max`** — общая площадь лота **м²** (`total_area`).\n'
    '- **`kitchen_min`**, **`kitchen_max`** — площадь кухни **м²**.\n'
    '- **`floor`** — точный номер этажа.\n'
    '- **`floor_min`**, **`floor_max`** — диапазон этажей.\n'
    '- **`finishing`** — подстрока в описании отделки лота.\n'
    '- **`is_assignment`** — переуступка: `true` / `false`.\n'
    '- **`is_two_level`** — двухуровневая планировка.\n'
    '- **`has_master_bedroom`** — наличие мастер-спальни.\n'
    '- **`is_apartments_legal`** — юридически апартаменты / соответствующий признак в карточке.\n\n'
    'Поле **`price_per_sqm`** в ответе заполняется на сервере при сохранении лота, если заданы цена и площадь.'
)

_LISTING_UNITS_SUMMARY_DESCRIPTION = (
    '**Назначение:** агрегированная сводка по лотам одного опубликованного объекта, сгруппированная по полю '
    '**`layout_label`** (одна строка аккордеона на сайте — например «1-комнатная»). Для каждой группы возвращаются: '
    '**`count`** (число лотов), **`area_min`** / **`area_max`** (по общей площади), **`price_min`** / **`price_max`** '
    '(цена), **`price_per_sqm_min`** (минимальная цена за м² среди лотов группы).\n\n'
    '**Доступ и родитель** — как у `GET .../units/`: только **опубликованный** `listing_pk`, иначе **404**.\n\n'
    '**Фильтры:** те же query-параметры, что у списка лотов. Сводка считается **после** применения фильтров '
    '(т.е. отражает только отобранное множество лотов).\n\n'
    'Лоты с пустым `layout_label` попадают в одну группу с пустой меткой.'
)

__all__ = (
    '_CATALOG_DESCRIPTION',
    '_AGENT_LIST_FILTERS_DESCRIPTION',
    '_LISTING_UNITS_DESCRIPTION',
    '_LISTING_UNITS_SUMMARY_DESCRIPTION',
    '_PROPERTY_BODY_DESCRIPTION',
    '_PROPERTY_DETAIL_DESCRIPTION',
    '_PROPERTY_TAGS_CATALOG_HINT',
)
